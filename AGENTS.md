# AGENTS.md

Boltlane — open-source, self-hostable residential-IP proxy network. Go peer clients (Windows/Linux/macOS/Raspberry Pi; mobile later), a coordinating server, and Bitcoin Lightning payouts. See `CONTEXT.md` for domain glossary.

## Workflow

**Never commit directly to `main`** — branch, push, open a PR. Commit as `citepulse-barret <barret@citepulse.io>` (see the CitePulse commit-identity policy).

## Agent skills

### Issue tracker

Issues live in GitHub at `CitePulse-io/boltlane`, accessed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) — defaults, no remapping. See `docs/agents/triage-labels.md`.
